import DashboardPlaceholder from "../DashboardPlaceholder";

export default function SettingsPage() {
  return (
    <DashboardPlaceholder
      icon="settings"
      title="Settings"
      description="This area will hold dashboard preferences and account settings after the user profile flow is finalized."
    />
  );
}
