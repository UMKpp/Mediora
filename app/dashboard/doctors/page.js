import DashboardPlaceholder from "../DashboardPlaceholder";

export default function DoctorsPage() {
  return (
    <DashboardPlaceholder
      icon="doctor"
      title="Doctors"
      description="This area will help users find doctors, view care options, and prepare for appointments when provider data is added."
    />
  );
}
