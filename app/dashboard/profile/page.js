import DashboardPlaceholder from "../DashboardPlaceholder";

export default function ProfilePage() {
  return (
    <DashboardPlaceholder
      icon="profile"
      title="Profile"
      description="This area will hold user account details, preferences, and health profile settings after authentication is connected."
    />
  );
}
